<routes xmlns="http://camel.apache.org/schema/spring">
    <route id="tracciaDati">
        <from uri="direct:tracciaDati" />
        <log message="Richiesta petalo Header ${headers} Body ${body} " loggingLevel="INFO"  />
    </route>
</routes>